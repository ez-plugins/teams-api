name: CI

on:
  push:
    branches:
      - main
  pull_request:
  release:
    types:
      - published

jobs:
  lint:
    name: Checkstyle Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
          cache: maven

      - name: Purge stale JitPack artifacts from Maven cache
        run: rm -rf ~/.m2/repository/com/github

      - name: Run Checkstyle
        run: mvn -B -ntp checkstyle:check

  build:
    name: Build & Test
    runs-on: ubuntu-latest

    services:
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
          cache: maven

      - name: Purge stale JitPack artifacts from Maven cache
        run: rm -rf ~/.m2/repository/com/github

      - name: Compile, test and coverage
        env:
          REDIS_HOST: localhost
          REDIS_PORT: 6379
        run: mvn -B -ntp verify

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: teams-api/target/site/jacoco/jacoco.xml
          flags: unittests
          fail_ci_if_error: true

  package:
    name: Package Plugin
    runs-on: ubuntu-latest
    needs: [lint, build]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
          cache: maven

      - name: Purge stale JitPack artifacts from Maven cache
        run: rm -rf ~/.m2/repository/com/github

      - name: Package
        run: mvn -B -ntp -DskipTests package

      - name: Upload plugin JAR
        uses: actions/upload-artifact@v4
        with:
          name: teams-api-plugin
          path: teams-api-plugin/target/teams-api-plugin-*.jar
          if-no-files-found: error

      - name: Upload velocity JAR
        uses: actions/upload-artifact@v4
        with:
          name: teams-api-velocity
          path: teams-api-velocity/target/teams-api-velocity-*.jar
          if-no-files-found: error

      - name: Upload BungeeCord JAR
        uses: actions/upload-artifact@v4
        with:
          name: teams-api-bungeecord
          path: teams-api-bungeecord/target/teams-api-bungeecord-*.jar
          if-no-files-found: error

      - name: Upload BetterTeams extension JAR
        uses: actions/upload-artifact@v4
        with:
          name: teams-api-extension-betterteams
          path: teams-api-extension-betterteams/target/teams-api-extension-betterteams-*.jar
          if-no-files-found: error

      - name: Upload Towny extension JAR
        uses: actions/upload-artifact@v4
        with:
          name: teams-api-extension-towny
          path: teams-api-extension-towny/target/teams-api-extension-towny-*.jar
          if-no-files-found: error

      - name: Upload KingdomsX extension JAR
        uses: actions/upload-artifact@v4
        with:
          name: teams-api-extension-kingdomsx
          path: teams-api-extension-kingdomsx/target/teams-api-extension-kingdomsx-*.jar
          if-no-files-found: error
